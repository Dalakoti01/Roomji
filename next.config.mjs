/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ["images.unsplash.com", "randomuser.me","images.pexels.com","res.cloudinary.com"], // 👈 add external domains here
  },
};

export default nextConfig;
